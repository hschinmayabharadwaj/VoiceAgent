# Python dependencies for the suicide detection model server
torch>=2.0.0
transformers>=4.30.0
fastapi>=0.100.0
uvicorn>=0.22.0
pydantic>=2.0.0
scikit-learn>=1.3.0
numpy>=1.24.0
python-multipart>=0.0.6

# Optional: For GPU support (if available)
# torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118